# Package skills

Skills are self-contained directories below this directory. The package-specific
development guidance is in `timeline-component/SKILL.md`.

Provider-specific compatibility paths are materialized into commits by the
guidance hooks when they are used during local development.

The shared `lgs-1920-component-delivery` skill is linked from the local Studio
guidance source and is materialized into this repository's commit.
